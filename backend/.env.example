# Application
APP_ENV=development
PORT=8080

# Database (PostgreSQL)
# Local Docker:
DATABASE_URL=postgres://hotel_user:hotel_password@localhost:5433/hotel_db?sslmode=disable

# Supabase (uncomment and replace when ready):
# DATABASE_URL=postgres://postgres.[PROJECT_REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
